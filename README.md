To run frontend: cd into web and run yarn start

To run backend: cd into server and run yarn dev

Note, you need to have postgresql installed. Change the ormconfig.json file with your psql credentials. 
